{
  "name": "spec-lifecycle",
  "description": "Bootstrap skill for the spec-lifecycle primitive: lifecycle-init installs the CLI, elicits seeding choices, and scaffolds a project for the staged, human-gated, spec-driven issue lifecycle (refine → design → plan → archive, OpenSpec on-disk format). The per-stage skills are installed into the governed repo by `lifecycle init`, not bundled here.",
  "version": "0.1.0",
  "author": {
    "name": "Kentra"
  },
  "homepage": "https://github.com/kentra-io/spec-lifecycle",
  "repository": "https://github.com/kentra-io/spec-lifecycle",
  "license": "MIT",
  "keywords": ["sdd", "spec-driven", "openspec", "lifecycle", "gates", "planning"]
}
